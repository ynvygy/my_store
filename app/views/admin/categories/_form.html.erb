<%= form_for(["admin", @category]) do |f| %>
  <% if @category.errors.any? %>
    <h2><%= pluralize(category.errors.count, "error")%> stopped this category from being saved"</h2>
    <ul>
      <% @category.errors.full_messages.each do |msg| %>
        <li> msg </li>
      <% end %>
    </ul>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>