<p>Name: <%= @product.name%></p>
<td><%= @product.price %></td>
<td><%= @product.stock %></td>
<p><%= button_to "Add to cart", line_items_path(product_id: @product), :class => "btn btn-primary" %></p>
<%= link_to "Back", root_path %>

